# `zai_python_helper` — API reference

<!-- AUTO-GENERATED by tools/gen_docs.py (issue #19). Do not edit by hand — regenerate with `make docs`. Source of truth = docstrings + type hints in src/zai_python_helper/. -->

Machine-readable reference for the **importable public API** (`__all__`, issue #18). Every signature below is read live from the source — this file is never out of sync with the code.

## Contents

- [`__version__`](#__version__)
- [`ModelMode`](#modelmode)
- [`ProviderSpec`](#providerspec)
- [`Region`](#region)
- [`DeltaKind`](#deltakind)
- [`FileDelta`](#filedelta)
- [`FileTag`](#filetag)
- [`PatchPlan`](#patchplan)
- [`plan_default`](#plan_default)
- [`plan_zai`](#plan_zai)
- [`postconditions`](#postconditions)
- [`base_url_for_region`](#base_url_for_region)
- [`Paths`](#paths)
- [`JsonBackend`](#jsonbackend)
- [`ShellBackend`](#shellbackend)
- [`ClaudeCodeStatus`](#claudecodestatus)
- [`StatusReport`](#statusreport)
- [`detect_status`](#detect_status)
- [`render_status`](#render_status)
- [`ConfigurationError`](#configurationerror)
- [`ProviderError`](#providererror)
- [`ValidationError`](#validationerror)
- [`ZaiPythonHelperError`](#zaipythonhelpererror)

---

<a id="__version__"></a>

### `__version__`

*constant — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/__init__.py#L37)*

Value: `0.1.0`

*(no docstring)*

---

<a id="modelmode"></a>

### `ModelMode`

*enum — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/domain.py#L12)*

Mode for model selection in Claude Code configuration.

Each mode represents a different strategy for specifying which model
Claude Code should use when talking to a provider.

Modes (by priority):
- ORIGINAL: Only ANTHROPIC_BASE_URL, let server decide (like @z_ai/coding-helper)
- DEFAULT: Use preset ANTHROPIC_DEFAULT_*_MODEL variables
- SELECT: User selects from predefined list of models
- CUSTOM: User provides custom model ID manually

Members:

- `ORIGINAL` = `'original'`
- `DEFAULT` = `'default'`
- `SELECT` = `'select'`
- `CUSTOM` = `'custom'`

---

<a id="providerspec"></a>

### `ProviderSpec`

*dataclass — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/domain.py#L31)*

Specification for a provider configuration.

This is a shared domain type that captures all information needed
to generate configuration patches for any tool (Claude Code, OpenCode, etc.).

Per ADR-001, this is IO-free — it's just data. The planner layer
transforms this into PatchPlans, and the IO layer applies them.

Attributes:

- `base_url`: `str`
- `model_mode`: `ModelMode`
- `api_key`: `str | None`
- `selected_model`: `str | None`
- `custom_model_id`: `str | None`
- `custom_model_name`: `str | None`
- `custom_model_description`: `str | None`
- `custom_capabilities`: `str | None`

#### `validate()`

```python
validate(self) -> bool
```

Validate that the ProviderSpec is consistent for its mode.

Returns True if valid, False otherwise.


---

<a id="region"></a>

### `Region`

*enum — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/regions.py#L11)*

Z.ai deployment region.

Members:

- `GLOBAL` = `'global'`
- `CHINA` = `'china'`

---

<a id="deltakind"></a>

### `DeltaKind`

*enum — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/planner/__init__.py#L34)*

The kind of mutation a `FileDelta` represents.

- `WRITE_JSON`: write parsed JSON (`content` is a `dict`). The
  backend serializes with `indent=2` + trailing newline.
- `WRITE_TEXT`: write raw text (`content` is a `str`).
- `NOOP`: the file already matches the desired state; nothing to do.
  Kept (rather than omitted) so `--dry-run` can report that the file was
  considered and intentionally left untouched.

Members:

- `WRITE_JSON` = `'write_json'`
- `WRITE_TEXT` = `'write_text'`
- `NOOP` = `'noop'`

---

<a id="filedelta"></a>

### `FileDelta`

*dataclass — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/planner/__init__.py#L64)*

A single file's intended mutation, addressed by semantic tag.

Attributes:

- `tag`: `FileTag`
- `kind`: `DeltaKind`
- `content`: `Any`


---

<a id="filetag"></a>

### `FileTag`

*enum — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/planner/__init__.py#L50)*

Semantic identifier for a managed file (decoupled from its path).

The planner addresses files by tag; the CLI maps a tag to a concrete
`~pathlib.Path` via `~zai_python_helper.paths.Paths`. This
keeps path resolution out of the pure planner (ADR-001) and out of the
PatchPlan data structure, so a plan is independent of `$HOME`.

Members:

- `SETTINGS` = `'settings'`
- `CLAUDE_JSON` = `'claude_json'`
- `ZSHRC` = `'zshrc'`

---

<a id="patchplan"></a>

### `PatchPlan`

*dataclass — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/planner/__init__.py#L82)*

An ordered list of file deltas describing a complete activation.

Per ADR-005, activating a tool touches up to three files and the plan is
fully validated BEFORE any write. The ordering is preserved so logging
and `--dry-run` output are deterministic; deltas do not depend on each
other's write order within a single plan.

Attributes:

- `deltas`: `tuple[FileDelta, ...]`
- `is_empty`: `bool`
- `has_writes`: `bool`

#### `delta_for()`

```python
delta_for(self, tag: FileTag) -> FileDelta | None
```

Return the delta for `tag`, or `None` if the plan omits it.


---

<a id="plan_default"></a>

### `plan_default()`

*function — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/planner/claude_code.py#L309)*

```python
plan_default(provider_spec: ProviderSpec, settings_doc: dict[str, Any] | None, zshrc_text: str) -> PatchPlan
```

Plan the `use default` reversion for Claude Code.

PURE inverse of `plan_zai` for the env/zshrc concerns:

- `settings.json`: remove every managed key (ZAI + model-mode), keep
  foreign keys, drop `env` if empty.
- `.claude.json`: NOT touched (we never "un-complete" onboarding).
- `.zshrc`: remove ONLY the owned block.

Idempotent: a second `use default` on the post-state is all-NOOP.

---

<a id="plan_zai"></a>

### `plan_zai()`

*function — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/planner/claude_code.py#L251)*

```python
plan_zai(provider_spec: ProviderSpec, region: Region, settings_doc: dict[str, Any] | None, claude_json_doc: dict[str, Any] | None, zshrc_text: str, auth_token: str) -> PatchPlan
```

Plan the `use zai` activation for Claude Code.

PURE: takes parsed documents + the resolved auth token (resolved by the
caller in the IO layer, never here) and returns a `PatchPlan` of
up to three deltas. Each delta is a NOOP when the file already matches
the desired state, so a second `use zai` on the post-state yields an
all-NOOP plan (idempotent).

---

<a id="postconditions"></a>

### `postconditions()`

*function — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/planner/claude_code.py#L354)*

```python
postconditions(region: Region, settings_doc: dict[str, Any] | None, zshrc_text: str) -> bool
```

True iff `settings.json` + `.zshrc` reflect an active `use zai`.

PURE predicate used by `status`/`doctor` to confirm the activation
took effect. Checks:

- `settings.json` → `env` has `ANTHROPIC_AUTH_TOKEN` and
  `ANTHROPIC_BASE_URL` equal to the region's Z.ai URL, and does NOT
  have `ANTHROPIC_API_KEY`.
- `.zshrc` carries our owned block (presence marker).

Note: this validates state, NOT secrets — it checks key presence and the
base URL, never the token value (which may be redacted upstream).

---

<a id="base_url_for_region"></a>

### `base_url_for_region()`

*function — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/core/planner/claude_code.py#L64)*

```python
base_url_for_region(region: Region) -> str
```

Return the Z.ai Anthropic-compatible base URL for `region`.

Pure lookup into the static region→URL map. Raises if the region is
somehow unknown (defensive; the enum makes this unreachable today).

---

<a id="paths"></a>

### `Paths`

*dataclass — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/paths.py#L29)*

Frozen bundle of every resolved filesystem path the tool touches.

Frozen so a `Paths` instance handed to a handler/backend cannot be
mutated to silently redirect writes — a tampering guard. All fields are
`pathlib.Path`; they are set exclusively by `from_home`, so an
instance can never be half-resolved.

Attributes:

- `claude_settings`: `Path`
- `claude_json`: `Path`
- `zshrc`: `Path`
- `ownership_json`: `Path`
- `lock_file`: `Path`
- `state_dir`: `Path`

#### `from_home()`

```python
from_home(cls, home: str | Path) -> Paths
```

Resolve all paths off `home` via pure arithmetic (no IO).

Accepts `str | Path` and coerces to `pathlib.Path`. Does NOT
validate existence, create directories, or read anything — it
succeeds on a non-existent home. Symlinks are left as-is (no
`.resolve()`).

The resolved paths:
- `claude_settings`   = `home / ".claude" / "settings.json"`
- `claude_json`       = `home / ".claude.json"`
- `zshrc`             = `home / ".zshrc"`
- `ownership_json`   = `home / ".zai-python-helper" / "ownership.json"`
- `lock_file`         = `home / ".zai-python-helper" / "lock"`
- `state_dir`         = `home / ".zai-python-helper" / "state"`

#### `default()`

```python
default(cls) -> Paths
```

Production entry point: `from_home(Path.home())`.

A one-line thin wrapper with no alternate resolution path.
Tests never call this — they inject `tmp_path` via `from_home`
directly; that naming split is what makes test isolation provable.


---

<a id="jsonbackend"></a>

### `JsonBackend`

*class — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/backends.py#L97)*

Atomic JSON document writer/reader.

Stateless: each call takes an explicit path. The merge semantics live in
the planner; this backend only reads (leniently) and writes (atomically).

#### `read()`

```python
read(path: Path) -> dict[str, Any] | None
```

Parse `path` as JSON → dict, or `None` if it does not exist.

An empty file is treated as `None` (no document). A malformed file
raises `ConfigurationError` rather than crashing with a bare
`JSONDecodeError`.

#### `write()`

```python
write(path: Path, doc: dict[str, Any]) -> None
```

Serialize `doc` to pretty JSON and write it atomically to `path`.

`indent=2` + `sort_keys=False` (preserve insertion order) + a
trailing newline. Sorted keys would shuffle user-owned keys on every
write, producing noisy diffs, so we keep insertion order.

#### `render()`

```python
render(doc: dict[str, Any]) -> str
```

Render `doc` to the exact on-disk text (for diffing in --dry-run).

Pure: the bytes this backend WOULD write, without touching the FS.


---

<a id="shellbackend"></a>

### `ShellBackend`

*class — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/backends.py#L151)*

Owned marker-fenced block writer for shell rc files (ADR-003).

Applies the PURE block transforms from `zai_python_helper.shell_block`
and writes the result atomically. Foreign lines round-trip untouched.

#### `read()`

```python
read(path: Path) -> str
```

Return the raw text of `path`, or `""` if it does not exist.

#### `install_block()`

```python
install_block(path: Path) -> bool
```

Install the owned block in `path`. Returns True if it was added.

Idempotent: if the block is already present, writes nothing and
returns False. Foreign lines are never modified. Creates the file if
absent.

#### `remove_block()`

```python
remove_block(path: Path) -> bool
```

Remove the owned block from `path`. Returns True if it was removed.

Idempotent: if the block is absent, writes nothing and returns False.
Foreign lines are never modified. A no-op if the file does not exist.

#### `render_with_block()`

```python
render_with_block(text: str) -> str
```

Pure: the text this backend WOULD write to install the block.

#### `render_without_block()`

```python
render_without_block(text: str) -> str
```

Pure: the text this backend WOULD write to remove the block.


---

<a id="claudecodestatus"></a>

### `ClaudeCodeStatus`

*dataclass — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/status.py#L93)*

Detected state of the Claude Code integration.

Attributes:

- `settings_present`: `bool`
- `zai_active`: `bool`
- `region`: `Region | None`
- `base_url`: `str | None`
- `key_var`: `str | None`
- `key_masked`: `str | None`
- `zshrc`: `ZshrcState`


---

<a id="statusreport"></a>

### `StatusReport`

*dataclass — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/status.py#L106)*

The full read-only status of all detected tools.

Attributes:

- `claude_code`: `ClaudeCodeStatus | None`


---

<a id="detect_status"></a>

### `detect_status()`

*function — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/status.py#L299)*

```python
detect_status(paths: Paths) -> StatusReport
```

Read all tool configs and return a `StatusReport`.

Read-only and side-effect free. Today only Claude Code is detected;
OpenCode/Crush/Factory Droid join after S6.

---

<a id="render_status"></a>

### `render_status()`

*function — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/status.py#L377)*

```python
render_status(report: StatusReport, stream, use_color: bool | None) -> str
```

Render a `StatusReport` to a single string.

---

<a id="configurationerror"></a>

### `ConfigurationError`

*class — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/errors.py#L28)*

Error in a configuration file or settings document.


---

<a id="providererror"></a>

### `ProviderError`

*class — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/errors.py#L36)*

Error in provider configuration or connection.


---

<a id="validationerror"></a>

### `ValidationError`

*class — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/errors.py#L32)*

Error in input or domain validation.


---

<a id="zaipythonhelpererror"></a>

### `ZaiPythonHelperError`

*class — [source](https://github.com/axisrow/zai_python_helper/blob/main/src/zai_python_helper/errors.py#L17)*

Expected helper error → one-line message + non-zero exit, no traceback.

Raised by services/backends layers when an anticipated failure occurs
(file not found, config invalid, region unresolvable, key missing).
Caught once in `zai_python_helper.__main__.main`. The contract:
print `error: <msg>` to stderr and exit non-zero; under `--debug`
re-raise so Python emits the full traceback for debugging.
